{
    "name": "Rust",
    "dockerFile": "Dockerfile",
    "settings": { 
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "forwardPorts": [8000],
    "postCreateCommand": ["rustup update", "rustup override set stable", "cargo build"],
    "extensions": ["rust-lang.rust"]
}
